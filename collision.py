import setup

# 충돌함수 구현



is_Collision = False

# def check_collision(count):
#     if setup.Puang.posX < (setup.obs_list[count].posX + setup.obs_list[count].width) and setup.Puang.posX + setup.Puang.width > setup.obs_list[count].posX:
        
#         if setup.Puang.posY + setup.Puang.height >= setup.obs_list[count].height:
#             is_Collision = True



# 충돌 확인
# for i in range(0,len(obs_list)):
   
#     if Puang.posX < (obs_list[i].posX + obs_list[i].width) and Puang.posX + Puang.width > obs_list[i].posX:
#         if Puang.posY + Puang.height > obs_list[i].height:
#             collision.is_Collision = True
# if collision.is_Collision == True:
#     break
